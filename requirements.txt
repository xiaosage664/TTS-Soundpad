edge-tts>=6.1.0
customtkinter>=5.2.0
Pillow>=10.0.0
aiohttp>=3.9.0
requests>=2.28.0

# 本地离线引擎（可选）
# Piper TTS --- 快速本地语音合成
piper-tts>=1.2.0

# GPT-SoVITS --- 语音克隆引擎（需要额外安装 PyTorch）
# 安装方式:
#   1. pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#   2. pip install git+https://github.com/RVC-Boss/GPT-SoVITS.git@v2
#   3. pip install soundfile
# GPT-SoVITS 依赖:
#   torch, torchaudio, transformers, numpy, scipy, soundfile, onnxruntime
